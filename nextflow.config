profiles {
    standard {
        singularity.enabled = true
    }

    gpu {
        process.container = 'environment.gpu.sif'
        singularity.enabled = true
    }

    cpu {
        process.container = 'environment.cpu.sif'
        singularity.enabled = true
    }
}