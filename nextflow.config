profiles {
    standard {
        singularity.enabled = true
    }

    gpu {
        process.container = 'gpu.sif'
        singularity.enabled = true
    }

    cpu {
        process.container = 'cpu.sif'
        singularity.enabled = true
    }
}